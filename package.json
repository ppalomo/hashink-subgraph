{
  "name": "hashink",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ hashink/rinkeby",
    "create-local": "graph create --node http://localhost:8020/ hashink/rinkeby",
    "remove-local": "graph remove --node http://localhost:8020/ hashink/rinkeby",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hashink/rinkeby"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
